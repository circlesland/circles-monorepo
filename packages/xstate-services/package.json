{
  "name": "@circlesland/xstate-service",
  "version": "1.0.0",
  "description": "A library that defines support for safe interaction",
  "svelte": "src/index.ts",
  "module": "dist/index.mjs",
  "main": "dist/index.js",
  "scripts": {
    "dev": "vite build -w",
    "build": "vite build",
    "test": "jest -u --watch"
  },
  "files": [
    "src",
    "dist"
  ],
  "dependencies": {
    "@circlesland/interfaces-channels": "1.0.0"
  }
}
