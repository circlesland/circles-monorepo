{
  "name": "@circlesland/root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.3.0",
    "vite": "^3.0.4",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8"
  },
  "dependencies": {
    "json-schema-to-typescript": "^11.0.2"
  },
  "scripts": {
    "generate-types": "json2ts -i 'packages/frame-app/schemas/*.json' -o 'packages/frame-app/types'"
  }
}
