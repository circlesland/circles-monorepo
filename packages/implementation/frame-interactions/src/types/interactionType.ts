/**
 * An enumeration that defines the type of interaction
 */
export enum InteractionType {
    Continue = 'CONTINUE',
    Skip = 'SKIP',
    Back = 'BACK',
    Submit = 'SUBMIT',
    Cancel = 'CANCEL',
    Propmpt = 'PROMPT' 
}